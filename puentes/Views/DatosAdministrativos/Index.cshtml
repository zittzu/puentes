@model IEnumerable<puentes.Models.DatosAdministrativos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnioReconstruccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estructuras.Identificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnioReconstruccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estructuras.Identificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DatosAdminId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DatosAdminId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DatosAdminId })
        </td>
    </tr>
}

</table>
