@model IEnumerable<puentes.Models.TrabajosEstandar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InspeccionComponentes.Calificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Unidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InspeccionComponentes.Calificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrabajoEstandarId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrabajoEstandarId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrabajoEstandarId })
        </td>
    </tr>
}

</table>
